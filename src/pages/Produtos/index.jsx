import { DefaultTemplate } from "../../components/DefaultTemplate"

export const Produtos = () => {
    return (
        <main>
            <DefaultTemplate>
                <h1>Produtos do sistema Quick Sales Pro</h1>
            </DefaultTemplate>
        </main>
    )
}